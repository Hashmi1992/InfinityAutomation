{
  "name": "infinityautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run cypress-test || npm run posttest",
    "cypress-test": "cypress run --browser chrome",
    "cypress-run": "cypress run",
    "intro": "echo Test Suite made by M. Asad Hashmi",
    "posttest": "npm run merge-reports && npm run generate-htmlreport",
    "merge-reports": "mochawesome-merge ./cypress/results/*.json -o ./cypress/results/MergedReport1.json",
    "generate-htmlreport": "marge ./cypress/results/MergedReport1.json --reportDir ./ --inline",
    "cypress:run": "cypress run --spec 'cypress/e2e/Application.cy.js'0"
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^15.5.0"
  },
  "devDependencies": {
    "cypress-multi-reporters": "^2.0.5",
    "lru-cache": "^11.2.2",
    "mocha": "^11.7.4",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.3.0"
  }
}
